version: '3'
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: mcac
  api:
    image: fisherhall/api:latest
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - "3000:3000"
    env_file:
      - ./.env.dev
    depends_on:
      - db
